#include<stdio.h>
#include<stdlib.h>	
int main(){
	
		int fib=0, quant, n1=0, n2=1, i=1;
		printf("Insira a quantidade de termos: ");
		scanf("%d",&quant);
		while(i<=quant){
		      fib = n1+n2;
		      n1 = n2;
		      n2 = fib;
		      i++;
		      printf("%d,",fib);
		}
		return 0;
}
